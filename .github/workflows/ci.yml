name: Deployment Test - Test Server

on:
  push:
    branches: [ master ]

jobs:
  deploy-test:
    name: Deploy to Test Server
    runs-on: ubuntu-latest

    steps:
      - name: Checkout (optional, for logging context)
        uses: actions/checkout@v4

      - name: Pull image from Docker Hub
        run: docker pull snowvita/frontend-angular:latest

      - name: Run container as test server
        run: |
          echo "🚀 Starting test server container..."
          docker run -d -p 4200:80 --name angular-test snowvita/frontend-angular:latest
          echo "⏳ Waiting for container to initialize..."
          sleep 10
          docker ps

      - name: Verify deployment
        run: |
          echo "🔍 Checking if the app is accessible..."
          if curl -f http://localhost:4200 > /dev/null 2>&1; then
            echo "✅ Deployment test successful — container is running properly!"
          else
            echo "❌ Deployment test failed — app did not start correctly."
            docker logs angular-test
            exit 1
          fi

      - name: Cleanup test container
        if: always()
        run: |
          echo "🧹 Cleaning up test container..."
          docker rm -f angular-test || true
